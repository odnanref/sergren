{% extends 'base.html.twig' %}

{% block title %}{{ "New" | trans }} {{"Media" | trans }}{% endblock %}

{% block body %}
    <h1>{{ "Create new" | trans }} {{ "Media" | trans }}</h1>

    {{ include('media/_form.html.twig') }}

    <a href="{{ path('media_index') }}">{{ "back to list" | trans }}</a>
{% endblock %}
